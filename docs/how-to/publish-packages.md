# Publish packages

Package publishing is a maintainer workflow, so the maintained documentation for that process now lives at [Releasing packages](/contributors/releasing).

The contributor guide keeps the release policy, CI expectations, and trusted-publishing setup in one place.
